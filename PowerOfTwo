class Solution {
public:
    bool isPowerOfTwo(int n) {

        bool result = false;
        
        for(int i = 0; i < 1000; ++i)
        {
            if(pow(2, i) == n)
            {
                result = true;
                break;
            }
        }
        
        return result;
    }
};
